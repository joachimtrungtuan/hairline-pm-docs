# Pilot Plans

This folder contains human-reviewable discovery and registration proposals created before active test cases or runtime scripts.

A pilot plan may allocate proposed identifiers, but those identifiers do not become active registry entries until the proposal receives explicit human approval and the approved material is created under `registry/`.

There is currently no pilot plan. The first `web-e2e-register` run starts from a clean slate.
